# 生产环境配置（独立部署时使用）
# 部署前务必把下面的地址换成你实际的后端域名（Render 部署后形如 https://student-management.onrender.com）
# 提示：在 Cloudflare Pages / Render 构建环境里设置同名环境变量 VITE_API_BASE_URL 会覆盖此文件的值，
# 因此也可以不改这个文件，直接在托管平台的构建环境变量里配置。
VITE_API_BASE_URL=https://student-management.onrender.com/api
