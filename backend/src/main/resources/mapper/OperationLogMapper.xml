<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.student.mapper.OperationLogMapper">

    <select id="countToday" resultType="int">
        SELECT COUNT(*) FROM operation_log WHERE create_time &gt;= DATE_SUB(NOW(), INTERVAL 1 DAY)
    </select>

    <delete id="deleteOldLogs">
        DELETE FROM operation_log WHERE create_time &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

</mapper>
